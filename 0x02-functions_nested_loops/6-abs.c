#include "main.h"
/**
 * _abs - computes the absolute value of an interger
 * @n: the int being checked
 *
 * Return: the abs of the int
 */

int _abs(int n)
{
	if (n >= 0)
		return (n);
	else
		return (-n);
}
